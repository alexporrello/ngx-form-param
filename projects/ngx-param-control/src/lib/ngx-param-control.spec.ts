// Stub spec retained for Angular CLI scaffold compatibility.
// All real tests live in serializers.spec.ts, param-control.spec.ts,
// and param-group.spec.ts.
describe('ngx-param-control (scaffold stub)', () => {
  it('placeholder — see dedicated spec files', () => {
    expect(true).toBe(true);
  });
});
